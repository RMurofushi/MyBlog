<%= form_with(model: massage, local: true) do |form| %>
  <% if massage.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(massage.errors.count, "error") %> prohibited this massage from being saved:</h2>

      <ul>
      <% massage.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :massage_title %>
  </div>

  <div class="field">
    <%= form.label :message %>
    <%= form.text_field :message, id: :massage_message %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
